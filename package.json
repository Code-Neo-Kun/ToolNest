{
  "name": "neotools-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @neotools/web",
    "build": "npm run build --workspace @neotools/web",
    "lint": "npm run lint --workspace @neotools/web",
    "validate:registry": "tsx scripts/validate-registry.ts"
  },
  "devDependencies": {
    "tsx": "4.20.2",
    "turbo": "^2.10.5"
  }
}
